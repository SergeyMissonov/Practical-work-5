# Указываем базовый образ
FROM ubuntu:20.04
# Устанавливаем рабочую директорию в контейнере
WORKDIR /app
# Копируем содержимое папки в рабочую директорию
COPY . /app
# Команда для запуска нашего скрипта
CMD ["bash","./scripttime.sh"]
